export interface LayoutChild {
  children?: React.ReactNode
}
export interface ClientInfo {
  name: string
  ruc: string
  contact: string
  phone: string
  cuota: string
  id?: string
  cobrado?: boolean
}
